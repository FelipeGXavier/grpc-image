syntax = "proto3";

package processor;

message Point {
    number pixelPoint = 1;
}

service ImageProcessor {
    rpc GetAveragePixel(stream Point) returns (AveragePixel) {}
}

message AveragePixel {
    number average = 1;
}